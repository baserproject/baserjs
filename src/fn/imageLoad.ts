/**
 * 画像を読み込む
 *
 * @version 1.0.0
 * @since 1.0.0
 * @param imgList img要素リスト
 * @return ランダムに入れ替えられた配列
 *
 */
export default async function scriptLoad (imgList: NodeListOf<HTMLImageElement>): Promise<void> {
	for (const img of Array.from(imgList)) {
		
	}
}
